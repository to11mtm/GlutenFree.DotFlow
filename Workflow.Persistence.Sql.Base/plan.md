# Consolidate shared logic in SQL Providers;

 - Base classes with overridable methods
 - Use dependency injection for database connections (Linq2Db)
 - Implement unit tests for base classes, using SQLite as the baseline implementation.
