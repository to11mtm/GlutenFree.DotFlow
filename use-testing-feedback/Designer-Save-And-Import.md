## Designer Save And Load Options

- Users would like a way to export and import workflows.
- We should have this in a semi-human readable format
  - Options to consider:
    - JSON
	  - Yaml (not preferred by stakeholders)
    - ??? (not sure if there's a better option)
- We should maintain the layout of the modules as shown in the Designer when they save
  - We do not need to maintain what the current zoom/viewport is, on import we should zoom to the full view.
