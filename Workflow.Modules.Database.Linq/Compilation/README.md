# `Compilation/` 🧠

Roslyn compile pipeline: codegen (`DynamicWorkflowContext`, `LinqInputs`, `WorkflowScript` wrapper), reference/usings/syntax whitelists, and HMAC signing. Lands in **2.4.b.1**; the blob-backed compiled-assembly cache lands in **2.4.b.2**.

*Placeholder marker for the 2.4.b.0 scaffold — delete once real files land.*

