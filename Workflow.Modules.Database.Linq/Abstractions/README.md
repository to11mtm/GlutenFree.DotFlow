# `Abstractions/` 🧩

Interfaces + request/result records for the typed linq family. Lands in **2.4.b.1** (`IWorkflowLinqCompiler`, `LinqCompileRequest`/`LinqCompileResult`, `LinqDiagnostic`) and **2.4.b.4** (`IWorkflowLinqPreviewer`).

*Placeholder marker for the 2.4.b.0 scaffold — delete once real files land.*

