# `Execution/` ⚙️

Collectible `AssemblyLoadContext` executor that loads a compiled body, runs `WorkflowScript.ExecuteAsync(db, inputs, ct)`, materialises rows (no ALC-rooted references escape — D8), and unloads. Lands in **2.4.b.3**.

*Placeholder marker for the 2.4.b.0 scaffold — delete once real files land.*

