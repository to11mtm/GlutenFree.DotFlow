# `Builtin/` 🌟

`LinqQueryModule` (`builtin.database.linq`) — the primary typed authoring surface (D12). Resolves the compiler + cache + ALC executor from DI and returns materialised rows. Lands in **2.4.b.3**.

*Placeholder marker for the 2.4.b.0 scaffold — delete once real files land.*

