# `Preview/` 🔎

`IWorkflowLinqPreviewer` — a rollback-only `:memory:` SQLite sandbox that runs a compiled body against seeded sample data and returns a bounded preview (never commits). Lands in **2.4.b.4** (with one-shot catalog import per Q17/D19).

*Placeholder marker for the 2.4.b.0 scaffold — delete once real files land.*

